import 'contact_widgets_imports.dart';

class SocialContactUsShimmerWidget extends StatelessWidget {
  const SocialContactUsShimmerWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
